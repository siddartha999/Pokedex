import React from "react";

export const statSelectedFromPokeCardContext = React.createContext(null);

export const selectedStatValueContext = React.createContext(null);

export const pickStatRandomlyValueContext = React.createContext(null);

export const pokemonTypeImageListContext = React.createContext(null);
